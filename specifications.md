# ニュース検索アプリ プログラム仕様書

## 1. 概要
本アプリケーションは、Google NewsのRSSフィードを利用して、特定のキーワードに関連する最新ニュース（本日～2日前）を検索し、一覧表示するWebアプリケーションです。

## 2. ファイル構成
- `index.html`: アプリケーションの画面構造（HTML）
- `styles.css`: アプリケーションのデザイン（CSS）
- `script.js`: ニュース取得・表示の制御ロジック（JavaScript）

## 3. 機能仕様

### 3.1 ニュース検索機能
- ユーザーが入力したキーワード（カンマ区切りで複数指定可能）に基づき、各キーワードごとにニュースを検索します。
- **検索対象**: Google News (RSSフィード)
- **検索範囲**: 実行時から2日前までのニュース
- **表示件数**: 各キーワードにつき最大5件

### 3.2 クリア機能
- 「クリア」ボタンを押下することで、表示されている検索結果および検索期間を消去します。
- **注意**: 入力フィールドの検索キーワードは保持されます。

### 3.3 バージョン管理機能
- プログラム内でバージョン定数（`VERSION`）および更新履歴（`VERSION_HISTORY`）を保持します。
- 画面タイトル横のバージョン番号をクリックすると、ポップアップ（モーダル）でバージョン履歴を確認できます。

### 3.4 取得期間の表示
- 検索実行時に、実際に検索対象となった期間を「期間：yyyy/mm/dd ～ yyyy/mm/dd」の形式で表示します。

### 3.5 フィルタリング
- ニュースの公開日時（`pubDate`）を判定し、現在から2日以上前のニュースは除外します。

### 3.6 外部APIの利用
- `https://news.google.com/rss/search`: Google NewsのRSSフィード取得用URL。
- `https://api.rss2json.com/v1/api.json`: RSSフィード(XML)をJSON形式に変換するために利用するプロキシサービス。

## 4. 画面設計 (UI)
- **入力フィールド**: 検索キーワードを入力するテキストボックス。初期値として以下のキーワードが設定されています。
  - Python, S＆P500, windows11, BTC, XRP, ETH, 仮想通貨, FXライブ, ipad, FX, chatGPT, Amazon, 暗号資産, 解説
- **操作エリア**:
  - **検索ボタン**: ニュース取得処理を開始。
  - **クリアボタン**: 検索結果のみを消去。
- **検索期間表示**: 検索ボタン押下後に表示される日時の範囲。
- **結果表示エリア**: キーワードごとに見出しを表示し、該当するニュースのタイトル（リンク付き）をリスト形式で表示。
- **バージョン履歴モーダル**: バージョンクリックで表示されるポップアップ。右上の「×」ボタンまたは画面外クリックで閉じます。

## 5. ロジック詳細 (script.js)

### 5.1 `isWithinLastTwoDays(pubDateStr)`
- 引数: 公開日時の文字列
- 処理: 現在時刻からマイナス2日の日付を計算し、引数の日時がその範囲内であれば `true` を返します。

### 5.2 `clearResults()`
- 処理: 検索結果エリア（`#results`）と期間表示エリア（`#searchPeriod`）の内容を空にします。

### 5.3 `toggleVersionHistory()`
- 処理: バージョン履歴モーダルの表示/非表示を切り替えます。表示時には `VERSION_HISTORY` からリストを動的に生成します。

### 5.4 `searchNews()`
1. 入力フィールドの値を取得し、配列化します。
2. 検索期間（2日前～本日）を算出し、画面に表示します。
3. 結果表示エリアをクリアします。
4. 各キーワードに対して非同期でニュースを取得し、フィルタリング（上位5件）して表示します。

## 6. スタイル定義 (styles.css)
- `button-group`: ボタンを横並びに配置（`display: flex`）。
- `version-badge`: タイトル横に小さく表示されるバッジ形式。
- `modal`: 画面中央に表示されるポップアップ。背景を半透明の黒（`rgba(0,0,0,0.5)`）で覆います。
- `search-period`: 検索期間を目立たせるためのフォント設定。
